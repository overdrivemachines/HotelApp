<h1>Editing Guest</h1>

<%= render 'form' %>

<%= link_to 'Show', @guest %> |
<%= link_to 'All Guests', guests_path %>
<%= link_to 'Cancel Reservation', property_reservation_path(current_user.property, @guest.reservation), method: :delete, data: { confirm: 'Are you sure?' }  %>
