<%= form_for(@room_type_rate) do |f| %>
  <% if @room_type_rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room_type_rate.errors.count, "error") %> prohibited this room_type_rate from being saved:</h2>

      <ul>
      <% @room_type_rate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :room_type_id %><br>
    <%= f.text_field :room_type_id %>
  </div>
  <div class="field">
    <%= f.label :on_date %><br>
    <%= f.date_select :on_date %>
  </div>
  <div class="field">
    <%= f.label :rate %><br>
    <%= f.text_field :rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
